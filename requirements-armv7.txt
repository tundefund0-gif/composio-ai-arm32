# Zen Agent ARM32 — ARMv7-tested dependencies
# These versions have been verified to work on armv7l (32-bit ARM)
# Install: pip install -r requirements-armv7.txt

httpx>=0.28.0
fastapi>=0.100.0,<1.0.0
uvicorn>=0.30.0
typer>=0.12.0
pydantic>=1.10.0,<2
websockets>=12.0
python-multipart>=0.0.9
aiofiles>=24.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
